Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Cors==4.0.1
Flask-JWT-Extended==4.6.0
SQLAlchemy==2.0.34
python-dotenv==1.0.1
python-docx==1.1.2
docxtpl==0.16.8
Pillow==10.4.0
qrcode==7.4.2
# pypdf: append the signature-certificate pages onto every PDF download so the
# downloaded file VISUALLY shows the signatures + QR even when the underlying
# signed DOCX bytes (which must stay untouched for cryptographic verification)
# do not. The merge happens on-the-fly at download time — no storage cost.
pypdf==5.1.0
bcrypt==4.2.0
gunicorn==22.0.0
cryptography==43.0.1
asn1crypto==1.5.1
gostcrypto==1.2.5
psycopg2-binary==2.9.9
